# antd-crm

Vite + React + Ant Design minimal CRM (Orders + Customers) with localStorage persistence.

## Install

```bash
npm install
npm run dev
```

Open the URL printed by Vite (usually http://localhost:5173).

Files are small and composed for quick tinkering. Edit pages/components under `src/`.